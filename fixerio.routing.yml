
fixerio.rates:
  path: '/admin/config/services/fixerio'
  defaults:
    _controller: '\Drupal\fixerio\Controller\DashboardController::rates'
    _title: 'Fixer.io'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
fixerio.settings:
  path: '/admin/config/services/fixerio/settings'
  defaults:
    _form: '\Drupal\fixerio\Form\SettingsForm'
    _title: 'Fixer.io settings'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
