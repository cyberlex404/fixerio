
fixerio.dashboard_rates:
  path: '/admin/config/fixerio/rates'
  defaults:
    _controller: '\Drupal\fixerio\Controller\DashboardController::rates'
    _title: 'Fixer.io rates'
  requirements:
    _permission: 'access administration pages'
fixerio.settings_form:
  path: '/admin/config/fixerio/settings'
  defaults:
    _form: '\Drupal\fixerio\Form\SettingsForm'
    _title: 'Fixer.io'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
